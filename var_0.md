# **Вариант № 0**

## **Генеративная художественная композиция "Городские ритмы" (ООП, Python)**

---

## **История**

Вы — **creative technologist** в студии медиа-арта. Ваш заказчик — музей современного искусства, который хочет получить интерактивную генеративную инсталляцию "Городские ритмы". Инсталляция должна создавать уникальные городские пейзажи в реальном времени, где каждое здание обладает своим характером, а посетитель может влиять на композицию через интерактивные элементы.

**Ключевая идея**: использовать ООП для того, чтобы сделать архитектурные элементы "живыми" — они должны уметь трансформироваться, комбинироваться и реагировать на внешние воздействия, создавая бесконечное разнообразие художественных форм.

---

## **Исходные данные**

Базовые параметры композиции:

```python
canvas_size = (800, 600)  # ширина, высота в пикселях
building_count = 8
```

Для хранения цветовой палитры используется **паттерн Borg**, гарантирующий, что все элементы композиции используют одну цветовую схему. Это позволяет мгновенно менять настроение всей инсталляции (день/ночь/закат) через единый объект.

---

# **Уровень 1 — базовая модель**

## **История**

На первом этапе ваш заказчик хочет увидеть прототип: базовые формы зданий, которые можно размещать на холсте и визуализировать. Важно показать, как разные типы зданий (обычные и башни) могут использовать общий интерфейс, но реализовывать свою уникальную визуальную логику.

---

## **1. Абстрактный базовый класс архитектурного элемента**

### **Задание**

Создайте абстрактный класс `ArchElement`, который определяет общий интерфейс для всех архитектурных элементов.

Класс должен содержать:
- конструктор для хранения координат (x, y), размера (width, height) и названия элемента
- абстрактный метод `draw(ax)` — отрисовка элемента на переданной оси matplotlib
- метод `transform(scale_x, scale_y)` — масштабирование элемента (возвращает self для цепочечных вызовов)

### **Шаблон кода**

```python
from abc import ABC, abstractmethod

class ArchElement(ABC):
    def __init__(self, x, y, width, height, name):
        # TODO: сохранить координаты, размеры и имя
    
    @abstractmethod
    def draw(self, ax):
        pass
    
    def transform(self, scale_x, scale_y):
        # TODO: изменить width и height с учетом масштаба
        # TODO: вернуть self
        pass
```

---

## **2. Классы-наследники архитектурных элементов**

### **Задание**

Создайте два класса, наследующихся от `ArchElement`:

- **`Building`** — прямоугольное здание с окнами (3 ряда по 3 окна)
- **`Tower`** — здание-башня с треугольной крышей и шпилем

### **Шаблон кода**

```python
class Building(ArchElement):
    def draw(self, ax):
        # TODO: отрисовать прямоугольник здания (patches.Rectangle)
        # TODO: добавить окна (вложенные прямоугольники)

class Tower(ArchElement):
    def draw(self, ax):
        # TODO: отрисовать прямоугольник основания
        # TODO: отрисовать треугольную крышу (patches.Polygon)
        # TODO: добавить шпиль (линия от вершины крыши)
```

---

## **3. Класс CityCanvas (итерируемый)**

### **Задание**

Создайте класс `CityCanvas`, который управляет коллекцией архитектурных элементов.

**Требования**:
- хранит список элементов в `self.elements`
- метод `add_element(element)` добавляет элемент
- метод `render()` отображает всю композицию
- класс должен быть итерируемым (реализовать `__iter__`)

### **Шаблон кода**

```python
class CityCanvas:
    def __init__(self, width, height):
        # TODO: сохранить размеры
        # TODO: создать пустой список elements
    
    def add_element(self, element):
        # TODO: добавить элемент в список
    
    def render(self):
        # TODO: создать фигуру matplotlib
        # TODO: установить пределы осей (0, width, 0, height)
        # TODO: для каждого элемента вызвать draw()
        # TODO: показать результат
    
    def __iter__(self):
        # TODO: вернуть итератор по self.elements
        pass
```

---

## **4. Паттерн Borg для управления цветом**

### **Задание**

Реализуйте класс `ColorTheme`, использующий **паттерн Borg** (все экземпляры разделяют состояние) для хранения цветовой схемы композиции.

**Требования**:
- все экземпляры класса имеют общий словарь `colors`
- метод `set_theme(theme_name)` — устанавливает тему ('day', 'night', 'sunset')
- метод `get(name)` — возвращает цвет по имени ('sky', 'building', 'window', 'roof')
- метод `update(name, color)` — обновляет конкретный цвет

### **Шаблон кода**

```python
class ColorTheme:
    _shared_state = {}
    
    def __init__(self):
        # TODO: установить self.__dict__ = self._shared_state
        # TODO: если словарь пуст, инициализировать базовые цвета
    
    def set_theme(self, theme_name):
        # TODO: установить цвета для day/night/sunset
    
    def get(self, name):
        # TODO: вернуть цвет по имени
    
    def update(self, name, color):
        # TODO: обновить конкретный цвет
```

---

## **5. Основной цикл создания композиции**

### **Задание**

Создайте композицию из 8 зданий (разные типы, разные размеры и позиции), используя созданные классы. Отобразите результат.

### **Шаблон кода**

```python
# создание цветовой темы
theme = ColorTheme()
theme.set_theme('day')

# создание холста
canvas = CityCanvas(800, 600)

# TODO: создать и добавить 8 элементов (Building и Tower)
# элементы должны быть на разных координатах, разного размера

canvas.render()
```

---

## **6. Визуализация через matplotlib**

### **Задание**

Реализуйте отрисовку элементов, используя библиотеку `matplotlib.patches`.

### **Шаблон кода**

```python
import matplotlib.pyplot as plt
from matplotlib.patches import Rectangle, Polygon

# В классе Building:
def draw(self, ax):
    # TODO: получить цвет здания из темы
    # TODO: создать Rectangle для фасада
    # TODO: в цикле создать Rectangle для окон
```

---

# **Уровень 2 — расширенная модель**

## **История**

Заказчик впечатлен прототипом, но хочет больше интерактивности. Теперь здания должны:
- Сохранять историю своих трансформаций (чтобы можно было отследить рост города)
- Иметь защиту от некорректных параметров (нельзя сделать здание с отрицательной высотой или разместить за пределами холста)
- Визуально демонстрировать изменения: цвет здания должен меняться в зависимости от высоты, окна появляться только на определенных этажах

**Дизайнерская задача**: Создать анимацию роста города, где здания растут неравномерно (разные здания растут на разных шагах), чтобы изменения были заметны визуально.

---

## **1. Инкапсуляция свойств элементов**

### **Задание**

Сделайте координаты и размеры приватными (`_x`, `_y`, `_width`, `_height`) с доступом через свойства (`@property`).

**Проверки**:
- x, y должны быть в пределах [0, ширина холста] и [0, высота холста]
- width, height должны быть положительными числами
- При изменении высоты автоматически пересчитывается количество этажей (`self.floors = height // 20`)

### **Шаблон кода**

```python
class Building(ArchElement):
    def __init__(self, x, y, width, height, name, canvas_width, canvas_height):
        # Сохраняем границы холста
        self.canvas_width = canvas_width
        self.canvas_height = canvas_height
        
        # Приватные атрибуты
        self._x = x
        self._y = y
        self._width = width
        self._height = height
        self.name = name
        self.history = [height]  # история изменений высоты
        self.floors = height // 20  # количество этажей
    
    @property
    def x(self):
        # TODO: вернуть _x
    
    @x.setter
    def x(self, value):
        # TODO: проверить тип (int или float)
        # TODO: проверить границы 0..canvas_width
        # TODO: сохранить предыдущее значение в history (опционально)
        # TODO: установить _x
    
    @property
    def y(self):
        # TODO: вернуть _y
    
    @y.setter
    def y(self, value):
        # TODO: проверить тип
        # TODO: проверить границы 0..canvas_height
        # TODO: установить _y
    
    @property
    def width(self):
        # TODO: вернуть _width
    
    @width.setter
    def width(self, value):
        # TODO: проверить тип
        # TODO: проверить value > 0
        # TODO: установить _width
    
    @property
    def height(self):
        # TODO: вернуть _height
    
    @height.setter
    def height(self, value):
        # TODO: проверить тип
        # TODO: проверить value > 0
        # TODO: сохранить предыдущее значение в history
        # TODO: установить _height
        # TODO: обновить self.floors = _height // 20
```

---

## **2. Методы трансформации элементов**

### **Задание**

Добавьте каждому классу-наследнику специфические методы трансформации.

**Для Building**:
- `add_floors(n)` — увеличивает высоту на n*30px (для заметности изменений)
- `get_color()` — возвращает цвет в зависимости от высоты (lightgray → gray → darkgray)

**В методе `draw()`**:
- Цвет здания должен зависеть от высоты
- Окна рисуются только если есть этажи (`floors > 0`)
- Каждый этаж имеет 2 окна
- Цвет окон чередуется (желтый/оранжевый)

### **Шаблон кода**

```python
class Building(ArchElement):
    def add_floors(self, n):
        # TODO: увеличить высоту на n * 30
        # TODO: вернуть self для цепочечных вызовов
    
    def get_color(self):
        # TODO: вернуть цвет:
        #   если height < 100: 'lightgray'
        #   если height < 200: 'gray'
        #   иначе: 'darkgray'
    
    def draw(self, ax):
        # TODO: получить цвет через get_color()
        # TODO: отрисовать прямоугольник здания
        # TODO: если есть этажи (floors > 0):
        #   - рассчитать размер окна
        #   - для каждого этажа:
        #       - для каждого окна (2 шт):
        #           - определить цвет окна (желтый/оранжевый)
        #           - отрисовать окно

class Tower(ArchElement):
    def add_spire(self, height):
        # TODO: добавить атрибут _spire_height
        # TODO: добавить запись в history
        # TODO: вернуть self
    
    def change_roof_style(self, style):
        # TODO: сохранить стиль в атрибут _roof_style ('pointed' или 'rounded')
        # TODO: вернуть self
```

---

## **3. Визуализация с анимацией**

### **Задание**

Создайте класс `AnimatedCity`, который поддерживает пошаговую анимацию трансформаций.

**Требования**:
- метод `save_frame()` — сохраняет текущие высоты всех зданий
- метод `animate(interval)` — показывает анимацию с задержкой между кадрами

**Важно**: анимация должна быть понятной в Google Colab, поэтому используйте `plt.pause()` и закрывайте фигуры после каждого кадра.

### **Шаблон кода**

```python
class AnimatedCity:
    def __init__(self, width, height):
        self.width = width
        self.height = height
        self.buildings = []      # список зданий
        self.frames = []         # список кадров (высоты на каждом шаге)
    
    def add_building(self, building):
        # TODO: добавить здание в список
    
    def record_frame(self):
        # TODO: сохранить текущие высоты всех зданий в self.frames
        pass
    
    def show_frame(self, step, heights):
        # TODO: создать фигуру matplotlib
        # TODO: установить пределы осей
        # TODO: добавить фон (небо и землю)
        # TODO: для каждого здания:
        #   - временно установить высоту из heights
        #   - вызвать draw()
        #   - вернуть исходную высоту
        # TODO: добавить заголовок с номером шага и высотами
        # TODO: показать фигуру
        # TODO: сделать паузу (plt.pause)
        # TODO: закрыть фигуру
    
    def animate(self, interval=1.5):
        # TODO: для каждого кадра в self.frames:
        #   - вызвать show_frame()
        pass
```

---

## **4. Основной цикл анимации**

### **Задание**

Создайте композицию из 6 зданий с разной начальной высотой (от 45 до 120px). Реализуйте анимацию роста в 8 шагов, где:
- Четные здания (индекс 0,2,4) растут на четных шагах
- Нечетные здания (индекс 1,3,5) растут на нечетных шагах
- Каждый шаг добавляет 1 этаж (30px)

После анимации выведите итоговые высоты зданий.

### **Шаблон кода**

```python
# создание цветовой темы (если используется)
theme = ColorTheme()
theme.set_theme('day')

# создание анимированного холста
canvas = AnimatedCity(800, 600)

# TODO: создать 6 зданий Building с разными параметрами:
#   - координаты: 50, 150, 260, 350, 450, 550
#   - ширина: 60, 70, 55, 50, 65, 60
#   - начальная высота: 60, 90, 45, 120, 75, 105
#   - имена: "B1", "B2", "B3", "B4", "B5", "B6"

# TODO: добавить все здания в canvas

# Сохраняем начальное состояние
canvas.record_frame()

# Анимация роста (8 шагов)
for step in range(1, 9):
    # TODO: для каждого здания:
    #   - если шаг четный и здание с четным индексом: добавить этаж
    #   - если шаг нечетный и здание с нечетным индексом: добавить этаж
    canvas.record_frame()

# Запуск анимации
canvas.animate(interval=1.5)

# Вывод итоговых высот
print("\nИтоговые высоты зданий:")
for building in canvas.buildings:
    # TODO: вывести имя и высоту здания
    pass
```

---

## **5. Пример ожидаемого результата**

После выполнения задания должна получиться анимация, где:
- Шаг 0: все здания имеют начальную высоту
- Шаг 1: растут нечетные здания (B2, B4, B6)
- Шаг 2: растут четные здания (B1, B3, B5)
- И так далее...
- На каждом шаге в заголовке отображаются текущие высоты
- Цвет зданий меняется от светлого к темному по мере роста
- Окна появляются только на этажах, цвет окон чередуется

**Визуальные отличия между шагами должны быть явными благодаря:**
- Разной скорости роста зданий
- Изменению цвета фасада
- Появлению новых окон на добавленных этажах
- Отображению числовых значений высот в заголовке

---


# **Уровень 3 — продвинутая система с интерактивной анимацией**

## **История**

Музей хочет установить инсталляцию, где посетители смогут наблюдать, как город "растет" на их глазах. Нужно создать анимацию, показывающую пошаговое добавление этажей и трансформацию зданий в 3D-пространстве. Важно показать разнообразие архитектурных форм: обычные здания и башни должны визуально отличаться.

---

## **1. Классы для 3D-элементов**

### **Задание**

Создайте базовый класс `ArchElement3D` и классы-наследники `Building3D` и `Tower3D`.

**Требования**:
- Каждый элемент хранит историю своих изменений в `self.path` (список высот)
- Для башни дополнительно хранить историю высоты шпиля в `self.spire_path`
- Метод `get_color()` возвращает цвет в зависимости от высоты (для зданий) или фиксированный цвет (для башен)

### **Шаблон кода**

```python
class ArchElement3D:
    def __init__(self, x, y, width, height, name):
        # TODO: сохранить координаты, размеры, имя
        # TODO: создать self.path = [height]


class Building3D(ArchElement3D):
    def get_color(self):
        # TODO: вернуть цвет:
        #   если height < 150: 'lightgray'
        #   если height < 250: 'gray'
        #   иначе: 'darkgray'


class Tower3D(ArchElement3D):
    def __init__(self, x, y, width, height, name):
        super().__init__(x, y, width, height, name)
        # TODO: создать self.spire_height = height * 0.3
        # TODO: создать self.spire_path = [self.spire_height]
    
    def get_color(self):
        # TODO: вернуть 'saddlebrown' (цвет основания)
    
    def get_spire_color(self):
        # TODO: вернуть 'gold' (цвет шпиля)
```

---

## **2. Функции создания 3D-мешей**

### **Задание**

Создайте две функции для визуализации элементов в plotly:

- `create_building_mesh(x, y, width, depth, height, color)` — создает параллелепипед (здание)
- `create_tower_mesh(x, y, width, depth, height, spire_height, base_color, spire_color)` — создает башню (основание + пирамидальная крыша)

**Подсказка**: для параллелепипеда нужно определить 8 вершин и 12 треугольников (6 граней). Для пирамиды — 5 вершин и 4 треугольника.

### **Шаблон кода**

```python
def create_building_mesh(x, y, width, depth, height, color):
    """Создает параллелепипед для plotly"""
    # 8 вершин
    vertices_x = [x, x+width, x+width, x, x, x+width, x+width, x]
    vertices_y = [y, y, y+depth, y+depth, y, y, y+depth, y+depth]
    vertices_z = [0, 0, 0, 0, height, height, height, height]
    
    # TODO: определить индексы граней (i, j, k)
    # TODO: вернуть go.Mesh3d


def create_tower_mesh(x, y, width, depth, height, spire_height, base_color, spire_color):
    """Создает башню: основание + пирамидальная крыша"""
    traces = []
    
    # TODO: создать основание через create_building_mesh
    # TODO: создать пирамидальную крышу (5 вершин, 4 треугольника)
    # TODO: вернуть список из двух Mesh3d
```

---

## **3. Основной цикл трансформаций**

### **Задание**

Создайте композицию из 6 элементов (3 здания + 3 башни) с разными координатами и начальными высотами.

Выполните 8 шагов трансформаций, где:
- **Все элементы растут каждый шаг**, но с разной скоростью
- Каждое здание имеет свой коэффициент роста (разные значения для наглядности)
- Для башен одновременно растет основание и шпиль (шпиль растет пропорционально основанию)

**Важно**: перед началом цикла инициализируйте `path` каждого элемента начальной высотой.

### **Шаблон кода**

```python
# Создание элементов (3 здания + 3 башни)
elements = [
    Building3D(1.0, 1.0, 0.8, 60, "B1"),
    Building3D(2.5, 1.0, 0.9, 70, "B2"),
    Building3D(4.0, 1.0, 0.7, 50, "B3"),
    Tower3D(5.8, 1.0, 0.8, 80, "T1"),
    Tower3D(7.3, 1.0, 1.0, 90, "T2"),
    Tower3D(8.8, 1.0, 0.9, 70, "T3")
]

# Коэффициенты роста для каждого элемента
growth_rates = [15, 20, 25, 12, 18, 22]

# TODO: инициализировать path начальными высотами

# Основной цикл трансформаций (8 шагов)
for step in range(8):
    for i, elem in enumerate(elements):
        # TODO: увеличить высоту элемента на growth_rates[i]
        # TODO: добавить новую высоту в path
        
        if isinstance(elem, Tower3D):
            # TODO: увеличить высоту шпиля пропорционально росту
            # TODO: добавить новую высоту шпиля в spire_path
```

---

## **4. Формирование кадров анимации**

### **Задание**

Создайте кадры для анимации, где каждый кадр показывает:
- Поверхность земли (зеленый цвет, низкая прозрачность)
- Все здания с текущей высотой из `path[step]` и соответствующим цветом
- Все башни с текущей высотой основания и шпиля из `path[step]` и `spire_path[step]`

**Важно**: начальный кадр (step=0) должен показывать элементы с минимальными (начальными) высотами.

### **Шаблон кода**

```python
frames = []
max_steps = len(elements[0].path)  # количество шагов

# Координаты земли
x_ground = np.arange(0, 11, 0.5)
y_ground = np.arange(0, 3.5, 0.5)
X, Y = np.meshgrid(x_ground, y_ground)

for step in range(max_steps):
    traces = []
    
    # Поверхность земли
    traces.append(go.Surface(
        x=X, y=Y, z=np.zeros_like(X),
        colorscale='Greens', showscale=False, opacity=0.5
    ))
    
    # TODO: для каждого элемента:
    #   - получить высоту из elem.path[step]
    #   - если Building3D: создать mesh через create_building_mesh
    #   - если Tower3D: создать mesh через create_tower_mesh
    
    frames.append(go.Frame(data=traces, name=f'step{step}'))
```

---

## **5. Интерактивная 3D-визуализация**

### **Задание**

Создайте фигуру plotly с кнопкой Play, как в задании с дронами.

**Требования**:
- Начальная фигура должна использовать данные из `frames[0]` (минимальные высоты)
- Кнопка Play запускает анимацию с интервалом 500 мс
- Настроена камера для удобного обзора (глаз в точке (1.5, 1.5, 1.2))
- Добавлены подписи осей и заголовок

### **Шаблон кода**

```python
# Создание фигуры (начальный кадр — минимальные высоты)
fig = go.Figure(data=frames[0].data, frames=frames)

# Кнопка Play
fig.update_layout(
    updatemenus=[dict(
        type='buttons',
        buttons=[dict(
            label='Play',
            method='animate',
            args=[None, {"frame": {"duration": 500, "redraw": True},
                         "fromcurrent": True, "transition": {"duration": 0}}]
        )]
    )],
    scene=dict(
        xaxis_title='X координата',
        yaxis_title='Y координата',
        zaxis_title='Высота',
        camera=dict(eye=dict(x=1.5, y=1.5, z=1.2)),
        aspectmode='manual',
        aspectratio=dict(x=1.2, y=0.5, z=0.8)
    ),
    title="Анимация роста города (здания и башни)",
    showlegend=False
)

fig.show()
```

---

## **6. Ожидаемый результат**

После выполнения задания должна получиться интерактивная 3D-анимация, где:
- На начальном кадре показаны здания с минимальными высотами
- При нажатии Play здания начинают расти с разной скоростью
- Башни имеют золотые шпили, которые тоже растут
- Камеру можно вращать мышью для обзора с разных сторон
